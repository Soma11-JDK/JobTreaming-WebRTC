//Global
const HOME = "/"; //전체 방 조회
const JOIN = "/join"; //방 입장
const EXIT = "/exit"; //방 퇴장
const STREAMER = "/streamer"; //스트리머
const VIEWERS = "/viewers"; //시청자

const routes = {
    home: HOME,
    join: JOIN,
    exit: EXIT,
    streamer: STREAMER,
    viewers: VIEWERS
};

export default routes;